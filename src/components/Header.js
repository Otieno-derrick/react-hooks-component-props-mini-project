import React from 'react';

function Header ({name}) {
  return (
    <div>
        <Header>
            <h1>
                {name}
            </h1>
        </Header>
    </div>
  )
}

export default Header;